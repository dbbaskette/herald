spring:
  application:
    name: herald-ui
  threads:
    virtual:
      enabled: true
  sql:
    init:
      mode: never

herald:
  ui:
    db-path: ${HERALD_DB_PATH:~/.herald/herald.db}
    skills-path: ${HERALD_SKILLS_PATH:~/.herald/skills}

server:
  port: 8080
  address: 0.0.0.0
