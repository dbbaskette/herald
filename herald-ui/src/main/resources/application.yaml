spring:
  application:
    name: herald-ui
  threads:
    virtual:
      enabled: true
  sql:
    init:
      mode: never

herald:
  memory:
    db-path: ${HERALD_MEMORY_DB_PATH:~/.herald/herald.db}

server:
  port: 8080
