# Herald — Environment Configuration
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# Then either source it manually or use the run script:
#   source .env && make dev
#   ./run.sh

# ── Required ─────────────────────────────────────────────────────────
ANTHROPIC_API_KEY=
HERALD_TELEGRAM_BOT_TOKEN=
HERALD_TELEGRAM_ALLOWED_CHAT_ID=

# ── Optional: Additional AI Providers ────────────────────────────────
# Provide API keys for any providers you want available.
# Only providers with keys will be enabled.
# OPENAI_API_KEY=
# GEMINI_API_KEY=
# OLLAMA_BASE_URL=http://localhost:11434
# OLLAMA_API_KEY=ollama
# LM Studio (local, OpenAI-compatible). Set the base URL to enable; the provider
# then appears in the model dropdown / `/model lmstudio`. Cuts API spend to $0.
# LMSTUDIO_BASE_URL=http://localhost:1234/v1
# LMSTUDIO_API_KEY=lm-studio

# ── Optional: Google Workspace CLI (gws) ─────────────────────────────
# OAuth client credentials from Google Cloud Console (APIs & Services → Credentials → OAuth 2.0 Client ID).
# .env is the single source of truth — ./run.sh syncs ~/.config/gws/client_secret.json to match.
# To authenticate after setting these: ./run.sh auth
# See docs/gws-setup.md for full setup instructions.
# GOOGLE_WORKSPACE_CLI_CLIENT_ID=
# GOOGLE_WORKSPACE_CLI_CLIENT_SECRET=
#
# ── Alternate auth methods (rare; pick AT MOST one) ──
# Use a pre-obtained OAuth2 access token instead of the OAuth flow. Highest priority.
# Useful for CI, scripted runs, or when minting tokens out-of-band. Tokens expire — refresh externally.
# GOOGLE_WORKSPACE_CLI_TOKEN=
#
# Point gws at a specific OAuth/service-account credentials JSON instead of ~/.config/gws/.
# Useful when keeping multiple GCP projects side-by-side or for service-account workflows.
# GOOGLE_WORKSPACE_CLI_CREDENTIALS_FILE=
#
# ── Headless / container deployments ──
# Default keyring uses the macOS Keychain (or system keyring on Linux). For headless servers
# or Docker containers where no keyring daemon is available, switch to the encrypted-file
# backend. When set to "file", gws also requires GOG_KEYRING_PASSWORD (or it'll prompt).
# GOOGLE_WORKSPACE_CLI_KEYRING_BACKEND=keyring   # keyring (default) | file
#
# Override the GCP project ID used for quota and billing (rarely needed).
# GOOGLE_WORKSPACE_PROJECT_ID=

# ── Optional: Model Selection ────────────────────────────────────────
# Choose which model to use for each role. Mix providers freely —
# just make sure you've provided the API key above for each provider you reference.
#
# HERALD_DEFAULT_PROVIDER=anthropic               # Boot-time provider: anthropic, openai, ollama, gemini
# HERALD_MODEL_DEFAULT=claude-sonnet-4-5         # Main agent model (for the default provider)
# HERALD_MODEL_HAIKU=claude-haiku-4-5            # Fast/cheap subagent tier
# HERALD_MODEL_SONNET=claude-sonnet-4-5          # Mid-tier subagent
# HERALD_MODEL_OPUS=claude-opus-4-5              # High-capability subagent tier
# HERALD_MODEL_OPENAI=gpt-4o                     # OpenAI subagent tier
# HERALD_MODEL_OLLAMA=llama3.2                   # Ollama (local) subagent tier
# HERALD_MODEL_GEMINI=gemini-2.5-flash           # Gemini subagent tier
# HERALD_MODEL_LMSTUDIO=qwen/qwen3.5-35b-a3b     # LM Studio (local) model id

# ── Optional: Agent Behavior ─────────────────────────────────────────
# HERALD_CRON_TIMEZONE=America/New_York
# HERALD_AGENT_PERSONA=
# HERALD_AGENT_SYSTEM_PROMPT_EXTRA=
# HERALD_AGENT_CONTEXT_FILE=~/.herald/CONTEXT.md
# HERALD_AGENT_MAX_CONTEXT_TOKENS=200000
# HERALD_PROMPT_DUMP=false                        # Set to true to dump full prompts to ~/.herald/prompt-dump/
# Autonomous mode: when false, Herald runs sudo / system-dir writes / pipe-to-shell
# WITHOUT a confirmation prompt (the catastrophic blocklist still always applies).
# Leave true to keep the prompt-before-risky-command behavior.
# HERALD_SHELL_REQUIRE_CONFIRMATION=true

# ── Optional: Web & Weather ──────────────────────────────────────────
# HERALD_WEB_SEARCH_API_KEY=
# HERALD_WEATHER_LOCATION=

# ── Optional: MeetingNotes integration ───────────────────────────────
# Herald ingests completed meetings in real time via a webhook the MeetingNotes
# app posts to http://localhost:8081/api/meetings/ingest (set that URL in the
# MeetingNotes Settings → Webhook card, template "compact"). The paths below are
# only used by the date-query backstop, which reads the app's durable output
# read-only; defaults match a standard MeetingNotes install.
# HERALD_MEETINGNOTES_DB_PATH=~/Documents/MeetingNotes/db.sqlite
# HERALD_MEETINGNOTES_DIR=~/Documents/MeetingNotes
# Daily backstop schedule (Spring cron). Re-ingests any meeting the webhook
# missed; silent when nothing was missed. Set to "-" to disable.
# HERALD_MEETINGNOTES_CATCHUP_CRON=0 0 18 * * *

# ── Optional: Server ─────────────────────────────────────────────────
# HERALD_SERVER_PORT=8080

# ── Optional: UI Paths ───────────────────────────────────────────────
# HERALD_SKILLS_PATH=~/.herald/skills
# HERALD_BUNDLED_SKILLS_PATH=/path/to/herald/skills
